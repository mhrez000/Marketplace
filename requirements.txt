# Core
Django>=5.1,<5.2
djangorestframework>=3.15
django-allauth>=65.0
django-environ>=0.11
dj-database-url>=3.0
Pillow>=10.0
whitenoise>=6.6

# Production server + Postgres driver (used by Docker/Fly + CI)
gunicorn>=22.0
psycopg[binary]>=3.2

# Added when their phase lands:
# celery>=5.4                 # background jobs (move housekeeping off-request)
# redis>=5.0                  # broker/cache
# stripe>=10.0                # real payments (set STRIPE_* env to activate)
# sentry-sdk>=2.0             # error tracking

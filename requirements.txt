# Core (Phase 0). Pin loosely now; tighten before deploy.
Django>=5.1,<5.2
djangorestframework>=3.15
django-allauth>=65.0
django-environ>=0.11
dj-database-url>=3.0
Pillow>=10.0
whitenoise>=6.6

# Added in later phases (kept here as a reference / install when needed):
# psycopg[binary]>=3.2        # Postgres driver (Phase 2+)
# celery>=5.4                 # background jobs (Phase 0/3+)
# redis>=5.0                  # broker/cache
# stripe>=10.0                # payments (Phase 3)
# gunicorn>=22.0              # prod server
# sentry-sdk>=2.0             # error tracking
